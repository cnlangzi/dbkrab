version: "2"

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude:
    - pattern: "SA4010: this result of append is never used"
      linter: staticcheck
