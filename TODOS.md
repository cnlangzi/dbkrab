# dbkrab 生产化待办事项

**创建时间**: 2026-04-03  
**优先级说明**: P0=阻碍生产, P1=生产前必须, P2=生产后尽快, P3=技术债

---

## P0 - 阻碍生产（本周完成）

### P0-1: CDC 清理保护机制 ✅ **已完成**
- [x] 实现 LSN 差距检测（对比当前消费位点与 CDC 表最小 LSN）
- [x] 配置告警阈值（如差距 > 1 小时或 > 100 万行）
- [x] 告警触发后暂停消费 + 发送通知（**第一阶段不做自动恢复**）
- [ ] 文档：SQL Server CDC 保留窗口配置指南
- [ ] 文档：人工恢复操作手册（DBA 决策后执行）

**验收标准**:
- [x] 模拟 CDC 清理场景，系统能检测到并告警
- [x] 告警后暂停消费，等待人工决策
- [x] **不自动执行全量快照**（避免生产库风险）

**完成时间**: 2026-04-03 (PR #4)

---

### P0-2: Offset 持久化加固
- [ ] 支持 Offset 写入 SQLite（与业务数据同库）
- [ ] 启动时从 CDC 系统表恢复 offset（如果文件丢失）
- [ ] 定期备份 offset 文件（如每小时）

**验收标准**:
- 删除 offset.json 后重启，能从 SQLite/CDC 恢复
- offset 写入与 Sink 写入在同一事务中（原子性）

---

### P0-3: Exactly-Once 语义验证
- [ ] 编写测试用例：Sink 写入失败时 offset 不推进
- [ ] 编写测试用例：Handler 失败不影响 offset（允许重试）
- [ ] 压力测试：模拟随机故障，验证无数据丢失

**验收标准**:
- 故障注入测试通过（无数据丢失、无重复消费）

---

### P0-4: 跨表事务完整性
- [ ] 实现事务缓冲池（buffer pool）
- [ ] 配置最大等待时间（如 30 秒）
- [ ] 超时事务强制交付（带警告日志）

**验收标准**:
- 跨 3 表的单事务变更，能完整交付

---

## P1 - 生产前必须（2 周内完成）

### P1-1: 重试机制
- [ ] 实现指数退避重试（初始 1s, 最大 60s, 重试 5 次）
- [ ] 区分可重试错误（网络超时、锁等待）与不可重试错误（语法错误、权限不足）
- [ ] 重试失败后进入 P1-2 死信队列

**验收标准**:
- 临时网络故障自动恢复，无需人工介入

---

### P1-2: 死信队列（DLQ）
- [ ] 创建 DLQ 表（SQLite/独立文件）
- [ ] 连续失败 N 次（如 5 次）的事务写入 DLQ
- [ ] 提供 DLQ 查询/重放 API

**验收标准**:
- 持续失败的事务不会阻塞主流程
- DLQ 数据可手动修复并重放

---

### P1-3: 监控指标（Prometheus） ✅ **已完成**
- [x] `dbkrab_poll_lag_seconds` - 消费延迟
- [x] `dbkrab_transactions_total` - 事务总数
- [x] `dbkrab_transactions_failed_total` - 失败事务数
- [x] `dbkrab_cdc_gap_bytes` - LSN 差距
- [x] `dbkrab_poll_duration_seconds` - 单次 poll 耗时
- [x] `dbkrab_alerts_total{level}` - 告警数量

**验收标准**:
- [x] Prometheus metrics endpoint: http://localhost:9021/metrics
- [ ] Grafana 面板展示核心指标

**完成时间**: 2026-04-03 (PR #5)

---

### P1-4: 密码管理
- [ ] 支持环境变量注入（`DBKRAB_DB_PASSWORD`）
- [ ] config.yml 支持 `password_env` 字段
- [ ] 文档：Kubernetes Secret / Vault 集成指南

**验收标准**:
- config.yml 无需存储明文密码

---

## P2 - 生产后尽快（1 个月内完成）

### P2-1: 自适应轮询
- [ ] 根据变更量动态调整间隔（变更多时缩短，变更少时延长）
- [ ] 配置最小/最大间隔（如 100ms - 5s）

---

### P2-2: 批处理控制
- [ ] 配置最大批处理事务数（如 1000 事务/批）
- [ ] 配置最大批处理字节数（如 10MB/批）

---

### P2-3: 结构化日志
- [ ] 迁移至 zerolog
- [ ] 添加 trace_id（跨事务追踪）

---

### P2-4: API 认证
- [ ] 添加 API Token 验证
- [ ] 配置 Token 文件（`/etc/dbkrab/api_token`）

---

## P3 - 技术债（后续迭代）

### P3-1: 集群支持
- [ ] 多实例部署（共享 offset 存储）
- [ ] 表级别分片（不同实例监控不同表）

---

### P3-2: 配置热加载
- [ ] 监听 config.yml 变化
- [ ] 安全重载（不中断正在处理的事务）

---

### P3-3: 优雅降级
- [ ] MSSQL 断连后进入重试循环（而非退出）
- [ ] 配置最大断连时间（超时后告警）

---

### P3-4: 集成测试
- [ ] Docker Compose 测试环境（MSSQL + dbkrab）
- [ ] 自动化测试：CDC 启用、变更捕获、故障恢复

---

## 完成记录

| 日期 | 任务 | 状态 | 备注 |
|------|------|------|------|
| 2026-04-03 | 创建待办清单 | ✅ | 基于方案审核报告 |
